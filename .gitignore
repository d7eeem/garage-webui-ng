# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env*
!.env.example
docker-compose.*.yml

data/
meta/
garage.toml

# Claude Code local state. `.claude/worktrees/` holds one full checkout of this
# repo per dispatched agent — currently ~1.4 GB — so this must stay ignored or
# a `git add -A` will try to commit the repo into itself.
.claude/
